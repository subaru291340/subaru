これで一応DB起動
mysqld

これだと普通にルートユーザとして動ける
mysql -u root -p

これでDB指定。それ以降はSQL投げれる
use mydb

py -m pip install ライブラリ名

py -m pip download ライブラリ名

py -m pip install --no-index-- whlファイル

pipdeptree

p

